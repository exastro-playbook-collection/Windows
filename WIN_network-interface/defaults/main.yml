---
#
# defaults file for interface
#

# VAR_WIN_network_interface:
  # - name: ""                              #インターフェース名
    # ipaddresses:
      # - ip: ""                            #IPアドレス(IPv6非対応)
        # prefix: 25                        #サブネットマスク長(bit)
        # gateway: ""                       #gateway
    # dhcp: false                           #DHCP有効/無効
    # netbios: "dhcp"                       #NetBIOS over TCP/IP
    # state: "Up"                           #状態(有効/無効)

# VAR_WIN_interface_ip: "{{ ansible_ip_addresses[0] }}"
# VAR_WIN_interface_ip: "1.1.1.1"
# VAR_WIN_interface_prefix: 25
# VAR_WIN_interface_dhcp: false

